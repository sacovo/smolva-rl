[project]
name = "lerobot_policy_smolvla_rl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "click>=8.3.2",
    "lerobot[smolvla]>=0.5.1",
    "marimo[lsp]>=0.23.2",
    "matplotlib>=3.10.8",
    "protobuf>=6.33.6",
    "scipy>=1.17.1",
    "seaborn>=0.13.2",
    "sentencepiece>=0.2.1",
    "tiktoken>=0.12.0",
    "tokenizers>=0.22.2",
    "torch>=2.10.0",
    "transformers>=5.3.0",
    "weasyprint>=68.1",
]
[build-system]
requires = ["uv_build>=0.11.7,<0.12"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
